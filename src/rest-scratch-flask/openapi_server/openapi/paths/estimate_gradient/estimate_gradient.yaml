post:
  tags: [math]
  operationId: estimate_gradient
  x-openapi-router-controller: openapi_server.controllers.math_controller
  description: "Description of the endpoint"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "estimate_gradient-input.yaml"
  responses:
    "200":
      description: "Description of this response."
      content:
        application/json:
          schema:
            $ref: "estimate_gradient-output.yaml"
