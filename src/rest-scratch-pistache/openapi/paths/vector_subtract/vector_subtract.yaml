post:
  tags: [math]
  operationId: vector_subtract
  x-openapi-router-controller: openapi_server.controllers.math_controller
  description: "Description of the endpoint"
  requestBody:
    content:
      application/json:
        schema:
          $ref: "vector_subtract-input.yaml"
  responses:
    "200":
      description: "Description of this response."
      content:
        application/json:
          schema:
            $ref: "vector_subtract-output.yaml"
