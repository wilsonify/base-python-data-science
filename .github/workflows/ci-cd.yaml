name: CI/CD

on:
  push:
    branches: [main, develop]
    tags:
      - 'v*.*.*'
  pull_request:
    branches: [main, develop]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-cd-${{ github.ref }}
  cancel-in-progress: true

env:
  IMAGE_NAME: ghcr.io/${{ github.repository }}/dsl
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  test:
    name: Run Python Tests
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install -r test-requirements.txt
          python -m pip install -r requirements.txt
          python -m pip install -e src/data-scratch-library

      - name: Run tests
        run: python -m pytest src/data-scratch-library/tests/ --cov=dsl --cov-report=term-missing --cov-report=xml --cov-branch --cov-fail-under=90

  build_image:
    name: Build and Validate Docker Image
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build Docker image tarball
        run: |
          docker buildx build \
            --file Dockerfile \
            --tag local/dsl:${{ github.sha }} \
            --output type=docker,dest=/tmp/dsl-image.tar \
            .

      - name: Load image from tarball
        run: |
          docker load --input /tmp/dsl-image.tar

      - name: Smoke test built image runtimes
        run: |
          docker run --rm local/dsl:${{ github.sha }} sh -c "python -c 'import dsl; print(\"python runtime: OK\")' && node --version && g++ --version"

      - name: Upload tested image artifact
        uses: actions/upload-artifact@v4
        with:
          name: dsl-image-tar
          path: /tmp/dsl-image.tar
          if-no-files-found: error
          retention-days: 7

  sonarqube:
    name: SonarQube Scan and Quality Gate
    runs-on: ubuntu-latest
    needs: build_image

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Java 17
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '17'

      - name: SonarQube Scan
        uses: SonarSource/sonarqube-scan-action@v8
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL || 'https://sonarcloud.io' }}
        with:
          args: >
            -Dsonar.organization=wilsonify

      - name: Enforce SonarQube Quality Gate
        uses: SonarSource/sonarqube-quality-gate-action@v1
        timeout-minutes: 10
        env:
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
          SONAR_HOST_URL: ${{ secrets.SONAR_HOST_URL || 'https://sonarcloud.io' }}
        with:
          scanMetadataReportFile: .scannerwork/report-task.txt

  publish:
    name: Publish Official Docker Image
    runs-on: ubuntu-latest
    needs: [build_image, sonarqube]
    if: github.event_name == 'push' && startsWith(github.ref, 'refs/tags/v')
    permissions:
      contents: read
      packages: write

    steps:
      - name: Download tested image artifact
        uses: actions/download-artifact@v4
        with:
          name: dsl-image-tar
          path: /tmp

      - name: Load tested image
        run: |
          docker load --input /tmp/dsl-image.tar

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Tag and push official image
        run: |
          VERSION_TAG="${GITHUB_REF_NAME#v}"
          docker tag local/dsl:${{ github.sha }} $IMAGE_NAME:$VERSION_TAG
          docker tag local/dsl:${{ github.sha }} $IMAGE_NAME:latest
          docker push $IMAGE_NAME:$VERSION_TAG
          docker push $IMAGE_NAME:latest
